services:
  redis:
    image: redis:7-alpine
    networks:
      - backend
    restart: unless-stopped

  php:
    image: wodby/drupal-php:8.3
    environment:
      PHP_FPM_USER: wodby
      PHP_FPM_GROUP: wodby
      PHP_MAX_EXECUTION_TIME: 300
      PHP_MAX_INPUT_VARS: 3000
      PHP_MEMORY_LIMIT: 512M
      # Sized for the BLS OEWS master XLSX (~80 MB) uploaded via the careers
      # refresh form. Bump in lockstep with NGINX_CLIENT_MAX_BODY_SIZE below.
      PHP_POST_MAX_SIZE: 128M
      PHP_UPLOAD_MAX_FILESIZE: 128M
      REVALIDATE_SECRET: ${REVALIDATE_SECRET}
      NEXTJS_REVALIDATE_URL: ${NEXTJS_REVALIDATE_URL}
    volumes:
      - ./drupal:/var/www/html
      - ./files/public:/var/www/html/web/sites/default/files
      - ./files/private:/var/www/private
    depends_on:
      - redis
    networks:
      - backend
    restart: unless-stopped

  varnish:
    image: wodby/varnish:6.0
    environment:
      VARNISH_SECRET: ${VARNISH_SECRET}
      VARNISH_BACKEND_HOST: ph-nginx
      VARNISH_BACKEND_PORT: 80
      VARNISH_CONFIG_PRESET: drupal
      VARNISH_PURGE_EXTERNAL_REQUEST_HEADER: X-Real-IP
    depends_on:
      - nginx
    networks:
      - backend
      - traefik_net
    labels:
      - traefik.enable=true
      - traefik.http.routers.programhub-drupal.rule=Host(`${DRUPAL_DOMAIN}`)
      - traefik.http.routers.programhub-drupal.entrypoints=websecure
      - traefik.http.routers.programhub-drupal.tls.certresolver=letsencrypt
      - traefik.http.services.programhub-drupal.loadbalancer.server.port=6081
      - traefik.docker.network=traefik_net
    restart: unless-stopped

  nginx:
    image: wodby/nginx:1.28
    environment:
      NGINX_STATIC_OPEN_FILE_CACHE: "off"
      NGINX_ERROR_LOG_LEVEL: debug
      NGINX_BACKEND_HOST: php
      NGINX_VHOST_PRESET: drupal10
      NGINX_SERVER_ROOT: /var/www/html/web
      NGINX_SERVER_NAME: ${DRUPAL_DOMAIN}
      # Match PHP_POST_MAX_SIZE / PHP_UPLOAD_MAX_FILESIZE on the php service.
      # Without this, the BLS XLSX upload returns 413 at the nginx layer
      # before it ever reaches PHP. Wodby's default is 1 MB.
      NGINX_CLIENT_MAX_BODY_SIZE: 128m
    volumes:
      - ./drupal:/var/www/html
      - ./files/public:/var/www/html/web/sites/default/files
    depends_on:
      - php
    networks:
      backend:
        aliases:
          - ph-nginx
    restart: unless-stopped

networks:
  backend:
    driver: bridge
  traefik_net:
    external: true
